interface:
  display_name: "Worklog"
  short_description: "Estimate hands-on hours per day from commit authorship"
  default_prompt: "Use $worklog to estimate my hands-on effort in this repository as a daily CSV."
policy:
  allow_implicit_invocation: false
