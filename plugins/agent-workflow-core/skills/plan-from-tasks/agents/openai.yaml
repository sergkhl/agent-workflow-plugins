interface:
  display_name: "Plan From Tasks"
  short_description: "Grill a task list into a committed implementation plan"
  default_prompt: "Use $plan-from-tasks to design and register a plan for these tasks."
policy:
  allow_implicit_invocation: false
