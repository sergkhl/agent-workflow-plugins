interface:
  display_name: "Drain Plans"
  short_description: "Work the plan index until nothing is actionable"
  default_prompt: "Use $drain-plans to complete the actionable plan index in validated, committed batches."
policy:
  allow_implicit_invocation: false
